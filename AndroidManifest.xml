<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GmiProspect"
        tools:targetApi="31">

        <!-- About Us Activity -->
        <activity
            android:name=".aboutus"
            android:exported="false"
            android:label="@string/title_activity_aboutus"
            android:theme="@style/Theme.GmiProspect" />

        <!-- Menu Activity (Launcher) -->
        <activity
            android:name=".menu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Course List Activity -->
        <activity android:name=".courselist" />

        <!-- Contact Us Activity -->
        <activity android:name=".contactus" />

        <!-- Eligibility Checker Activity -->
        <activity
            android:name="gmi.harith.gmiprospect.eligibilitychecker"
            android:exported="false" />

    </application>
</manifest>
